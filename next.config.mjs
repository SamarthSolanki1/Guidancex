/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ih1.redbubble.net','i.pinimg.com','github.com','randomuser.me'],
      }
  }

export default nextConfig;
